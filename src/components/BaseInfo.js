import React, { Component } from 'react';

import './baseinfo.less';

class BaseInfo extends Component {
    constructor() {
        super();
    }

    render() {
        return (
            <div className="abs-center base-info">Base info.</div>
        );
    }
}

export default BaseInfo;